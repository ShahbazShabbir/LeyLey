<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leyley.shoppingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeyLey">
        <activity android:name=".Activites.Payment"></activity>
        <activity android:name=".Activites.Categories" />
        <activity android:name=".Activites.OrderSubmited" />
        <activity android:name=".Activites.Notification" />
        <activity android:name=".Activites.BestSell" />
        <activity android:name=".Activites.Features" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".test.Test1" />
        <activity android:name=".WellcomeScreen" />
        <activity android:name=".Activites.Login" />
        <activity android:name=".Activites.Signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>